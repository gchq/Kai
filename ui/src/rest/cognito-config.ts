// Enter the generated IDs after cdk deploymeny and User Pool has been created.
// The following config details can be found at:
export const poolData = {
    UserPoolId: 'eu-west-2_123456', // Cognito > User Pools > KaiUserPool... > (on homepage) Pool Id: e.g. eu-west-2_aBc123
    ClientId: 'abc123', // Cognito > User Pools > KaiUserPool... > (left nav column) General Settings > App Clients > App client Id
};
